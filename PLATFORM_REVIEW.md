# 🛡️ مراجعة تطويرية — منصة «مرشد السلامة»
### كمنصة و SaaS · من زاوية التصنيفات · الأدوات · لوحات المؤسس
> التاريخ: يوليو ٢٠٢٦ — مراجعة مصحوبة بإصلاحات سريعة مُنفّذة فعلياً في هذا الفرع.
> نسخة تفاعلية (Artifact) متوفرة أيضاً بتصميم بصري.

---

## بطاقة النضج

| المحور | الدرجة | الخلاصة |
|--------|:------:|---------|
| التصنيفات (Taxonomy) | 4/10 | تحتاج أساساً — الأضعف بنيوياً |
| الأدوات (Tools) | 7/10 | قوية ومتنوّعة |
| لوحات المؤسس (Founder) | 6/10 | قوية، لكن أمانها معلّق على RLS |
| جاهزية SaaS | 3/10 | مبكّرة — لا باقات/صلاحيات/قياس |

---

## ما الذي أُصلح الآن (في هذا الفرع)

1. **حذف لوحة الأدمن القديمة** — كانت محمية بهاش عميل تافه `79b2b44a` قابل للاستخراج من الكود. أُزيلت بالكامل (JS + HTML + CSS). اللوحة الحديثة (Supabase Auth) تبقى وحدها.
2. **تحصين بروكسي الـ AI** — `supabase/functions/openai-proxy/index.ts`: قائمة نماذج مسموحة (`gpt-4o-mini`, `gpt-4o`) + سقف `max_tokens` (4000) + تصفية الحقول المُمرَّرة. يمنع إساءة التكلفة عبر طلب مُعدَّل.
3. **ملف سياسات RLS** — `supabase/migrations/0001_rls_policies.sql` جاهز للتطبيق: بيانات كل مستخدم مقصورة عليه، `founder_config` يُكتب من المؤسس فقط، `platform_secrets` بلا وصول للعميل. **يُطبَّق يدوياً من محرر Supabase.**
4. **تفعيل فلترة الأسئلة بالتصنيف** — منطق التصفية بالموضوع في محاكي كوادر كان مبنياً بالكامل لكن حاويته `kw-topic-chips` مفقودة من الصفحة، فكان معطّلاً بصمت. أُضيفت الحاوية + استدعاء التحديث عند فتح تبويب الاختبار.
5. **محرّر أسئلة المؤسس + إصلاح الحذف** — قائمة مواضيع تلقائية (`datalist`) واختيار المعيار بدل النص الحر؛ وإصلاح خلل عدم حفظ حذف الأسئلة المدمجة عبر `FCONFIG.deletedBuiltins` (يُطبَّق عند التحميل).

---

## ١. التصنيفات — الأضعف بنيوياً

- **لا سجل تصنيفات مركزي:** `topic`/`std` نص حرّ مبعثر عبر ~١٠٩ أسئلة → تشظٍّ حتمي (رصدنا ٥١ موضوعاً فريداً، كثير منها متقارب: «إدارة المخاطر» / «تقييم المخاطر» / «المخاطر»). **الحل:** جدول `categories` بمعرّفات ثابتة، والسؤال يشير بـ `topic_id`.
- **كل المحتوى مصفوفات JS ثابتة** (`KWB`, `FLASH`, `WEEKS`, القطاعات) → التوسّع يتطلب تعديل كود ونشراً. الأسئلة المُضافة تُحشر داخل مدوّنة `founder_config` واحدة بلا صعوبة/مصدر/وسائط/نسخ. **الحل:** جدول `questions` في DB + استيراد CSV/PDF.
- **الوسوم غير مُستغلّة في التصفح** — عولج جزئياً الآن (فلترة الموضوع). التالي: بحث + تصفية بالمعيار والصعوبة + إحصاء أداء لكل تصنيف.

## ٢. الأدوات — قوية ومتنوّعة (١١ أداة)

مزيج جيد: محلي (كوادر، مصفوفة المخاطر، هرم الضوابط، الخطة، المستويات) و AI (المرشد، محلل الصور Vision، بلاغ الحوادث، محلل المواقف، التقرير الأسبوعي، TBT).

**الفجوات:**
- تكرار كود (البطاقات ومحلل المواقف مكرّران) → صيانة مضاعفة.
- لا أداة PTW/تصريح عمل حقيقية رغم كونها من صميم HSE (سؤال + شريحة شات فقط).
- لا تصدير PDF للتقارير (HTML فقط).
- الشهادات غير قابلة للتحقق (لا رمز/QR).
- لا بوابة per-plan — الأدوات تُخفى عالمياً عبر `FCONFIG.tools` (شرط أساسي لـ Freemium).

## ٣. لوحات المؤسس — قوية، أمانها معلّق على RLS

**القوة:** لوحة ٨ تبويبات config-driven، مصادقة حقيقية، والحدّ اليومي للـ AI مُفروض خادمياً في الـ Edge Function.

**الفجوات:** التحليلات سطحية — لا إيراد، لا قمع تحويل، لا بقاء/أفواج، لا استخدام لكل أداة، لا سجل تدقيق. وإدارة المستخدمين للقراءة فقط (لا حظر/إعادة/منح باقة).

## ٤. الأمان — مرتّب بالخطورة

1. **حرِج — RLS غير مطبّقة:** احتمال قراءة عامة لبيانات كل المستخدمين + كتابة `founder_config` من أي مستخدم مصادَق (رفع حد الـ AI/تغيير اللاندنق). **أصدرنا ملف الحل.**
2. **مهم — تجاوز تأكيد البريد في `confirm-user`:** يؤكّد أي مستخدم أُنشئ خلال ١٥ دقيقة بالبريد فقط. تُرك توصيةً (يمسّ تدفق تسجيل حيّ): إمّا إلغاؤه والاعتماد على تأكيد Supabase، أو تقييده.
3. **أُصلح — بروكسي AI بلا allowlist** (خطر تكلفة).
4. **أُصلح — لوحة أدمن قديمة بهاش عميل ضعيف.**

---

## خارطة التحوّل إلى SaaS — مزيج مرحلي

- **الآن (Lead-gen):** أضِف Analytics (Plausible/GA)؛ أبرِز أن كوادر مجاني بلا حساب كطُعم + CTA «جرّب سؤالاً»؛ صفحة «نبذة» كاملة + دومين احترافي.
- **المرحلة ١ — الأساس:** نقل الأسئلة/البطاقات/القطاعات إلى DB مع سجل تصنيفات مركزي + بحث/تصفية/استيراد.
- **المرحلة ٢ — Freemium (دفع مؤجّل):** جدول `plans`/`entitlements`؛ بوابة الأدوات وحدّ الـ AI حسب الباقة؛ لوحة إيراد/قمع/استخدام لكل أداة (تجهيز فقط).
- **المرحلة ٣ — B2B:** Multi-tenancy (منظمات/مقاعد/أدوار) + تقارير على مستوى المنشأة + تصدير PDF + شهادات قابلة للتحقق (QR).
- **المرحلة ٤ — التشغيل:** تفكيك المونوليث (٣٥٠٠ سطر) + اختبارات + محتوى SEO + i18n كامل.

## الأولويات (أثر × جهد)

| العمل | الأثر | الجهد | الحالة |
|-------|:-----:|:-----:|--------|
| تطبيق سياسات RLS | عالٍ | منخفض | **عاجل — طبّق الملف** |
| إضافة Analytics | عالٍ | منخفض | التالي |
| فلترة التصنيف | متوسط | منخفض | ✅ تم |
| إزالة لوحة الأدمن القديمة | متوسط | منخفض | ✅ تم |
| تحصين بروكسي الـ AI | متوسط | منخفض | ✅ تم |
| نقل الأسئلة إلى DB | عالٍ | متوسط | المرحلة ١ |
| باقات + صلاحيات | عالٍ | متوسط | المرحلة ٢ |
| تصدير PDF + شهادات موثّقة | عالٍ | متوسط | المرحلة ٣ |
| B2B multi-tenancy | عالٍ | عالٍ | المرحلة ٣ |

---

## الخلاصة

عالِج **ثلاثاً** لتنتقل من «أداة شخصية جميلة» إلى «SaaS تشغيلي»: **طبّق RLS** (الملف جاهز)، **انقل التصنيفات إلى قاعدة البيانات**، و**أضِف الصلاحيات**. الأدوات واللوحة والعلامة الشخصية أصولٌ جاهزة — البنية التحتية هي ما ينقصها.
