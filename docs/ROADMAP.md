# خارطة الطريق — البنود المعلّقة

مُقطَّرة من المراجعة التسويقية (يونيو 2026) والمراجعة التقنية (يوليو 2026) — النص الكامل للمراجعتين محفوظ في تاريخ git. البنود المنجزة (إزالة اللوحة القديمة، تقوية وسيط AI، فلترة مواضيع كوادر، محرر الأسئلة، مزامنة Supabase) غير مدرجة.

## 🔴 عاجل — أمان

- [ ] **تطبيق سياسات RLS**: الملف `supabase/migrations/0001_rls_policies.sql` جاهز لكنه غير مطبَّق — يُنفَّذ يدوياً من محرر SQL في Supabase. أثر عالٍ، جهد منخفض، وهو الشرط قبل أي استخدام فعلي.
- [ ] **دالة `confirm-user`**: تؤكد أي بريد أُنشئ خلال 15 دقيقة بلا إثبات ملكية (تجاوز لتأكيد البريد). القرار: إلغاؤها والاعتماد على تأكيد Supabase الرسمي، أو تقييدها بتوكن يُصدر وقت التسجيل.

## 🟠 قياس ونمو

- [ ] إضافة Analytics (‏Plausible أو GA) — لا يوجد أي قياس حالياً.
- [ ] نطاق احترافي مخصص بدل نطاق Netlify الفرعي.
- [ ] وعد قيمة حاد في صفحة الهبوط موجّه لمتقدمي اختبار كوادر، مع إبراز أن المحاكي يعمل بلا أي مفتاح.

## 🟡 مصداقية

- [ ] استبدال التزكيات (testimonials) غير الحقيقية بشهادات فعلية أو حذفها.
- [ ] صورة حقيقية وسيرة موسّعة للمؤسس مع روابط LinkedIn.
- [ ] ذكر مصادر الأرقام والإحصائيات المعروضة.

## 🟢 بنية المحتوى (المرحلة 1)

- [ ] نقل بنك الأسئلة والبطاقات من الكود إلى قاعدة البيانات، مع سجل تصنيفات مركزي (`categories` + `topic_id`) بدل النص الحر (~51 موضوعاً متشظياً حالياً).
- [ ] بحث وتصفية بالمعيار (OSHA/ISO/NEBOSH) والصعوبة.

## 🔵 SaaS (المرحلة 2)

- [ ] جداول `plans` و`entitlements` وبوابة أدوات لكل باقة، وحد استخدام AI حسب الباقة بدل الحد الموحّد.
- [ ] لوحة مؤسس تشغيلية: إيراد، قمع تحويل، بقاء، وإدارة مستخدمين فعلية.

## 🟣 B2B ومخرجات (المرحلة 3)

- [ ] Multi-tenancy: منظمات، مقاعد، أدوار، ولوحة لمسؤول السلامة في المنشأة.
- [ ] تصدير PDF حقيقي للتقارير (بدل مستند Word-HTML).
- [ ] شهادات قابلة للتحقق: سجل شهادات في القاعدة + صفحة تحقق عبر QR.
- [ ] أداة تصاريح عمل (PTW) فعلية بسير اعتماد.

## ⚙️ تشغيل وجودة

- [ ] تفكيك المونوليث تدريجياً (فصل JS/CSS، ثم وحدات) مع اختبارات.
- [ ] محتوى SEO متجدد (نصائح/مقالات) وحملة LinkedIn منتظمة.
- [ ] استكمال i18n (الإنجليزية جزئية حالياً).
