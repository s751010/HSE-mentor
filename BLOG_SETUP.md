# 📰 دليل تشغيل مدونة HSE — خطوة بخطوة

تم تنفيذ كل الجزء البرمجي والخلفي تلقائياً. هذا الملف يغطي فقط **الخطوات اليدوية المتبقية** التي تحتاج تدخلك (معظمها تتم مرة واحدة).

---

## ✅ ما تم تنفيذه تلقائياً (لا يحتاج تدخلاً)
- **قاعدة البيانات:** جدول `articles` في Supabase + سياسات RLS (العامة تقرأ المنشور فقط، المؤسس وحده يكتب) + عدّاد مشاهدات آمن. *(مُختبر ويعمل)*
- **الذكاء الاصطناعي:** دالة Edge `generate-article` منشورة على Supabase (للمؤسس فقط، تعيد استخدام مفتاح OpenAI الموجود). *(منشورة)*
- **لوحة المؤسس:** تبويب «📰 المدونة» (توليد بالـAI + محرر + معاينة + نشر/مسودة/حذف).
- **المدونة العامة (SSR):** `netlify/edge-functions/blog.ts` لمسار `/blog` و`/blog/<slug>` مع كامل وسوم SEO وOpen Graph وschema.org JSON-LD.
- **محركات البحث:** `sitemap.xml` و`robots.txt` ديناميكيان (دالتا Edge).
- **روابط المدونة** في تنقّل اللاندنق والفوتر.
- **مقال افتتاحي منشور** (دليل LOTO) جاهز للعرض.

---

## 🔧 الخطوات اليدوية المطلوبة منك

### 1) دمج الفرع للنشر (ضروري)
كل العمل على فرع `claude/dreamy-sagan-yqubwf`. لكي تظهر المدونة على الموقع المباشر:
1. ادمج الفرع في فرع النشر (عادةً `main`) عبر Pull Request على GitHub.
2. Netlify يبني وينشر تلقائياً عند الدمج (إن كان مربوطاً بالمستودع).

> دوال Netlify Edge في مجلد `netlify/edge-functions/` تُكتشف وتُفعّل **تلقائياً** عند النشر — لا إعداد إضافي في لوحة Netlify.

---

### 2) التأكد أن Netlify مربوط بالمستودع (مرة واحدة)
- افتح netlify.com → موقعك → **Site configuration → Build & deploy**.
- تأكد أن المستودع `s751010/hse-mentor` مربوط، وأن النشر التلقائي مفعّل على فرع النشر.
- لا حاجة لتفعيل Edge Functions يدوياً — تأتي مفعّلة على كل خطط Netlify.

---

### 3) إضافة صورة المشاركة (موصى به للـSEO الاجتماعي)
عند مشاركة مقال على واتساب/تويتر/لينكدإن تظهر صورة معاينة. أضف صورة باسم:
```
/blog-og.png   (الأبعاد المثالية: 1200×630)
```
ضعها في **جذر المستودع** (بجانب `murshid.html`). صمّمها ببراند المنصة (شعار + «مدونة مرشد السلامة»).
> اختياري: يمكنك لاحقاً تحديد صورة مخصّصة لكل مقال عبر حقل `og_image` في قاعدة البيانات.

---

### 4) ربط Google Search Console (لظهورك في قوقل)
1. ادخل [Google Search Console](https://search.google.com/search-console) وأضف نطاق موقعك.
2. وثّق الملكية (Netlify يدعم التوثيق عبر DNS أو وسم HTML).
3. من **Sitemaps** أرسل الرابط:
   ```
   https://نطاقك/sitemap.xml
   ```
4. (اختياري) اطلب فهرسة فورية لأي مقال جديد عبر **URL Inspection → Request Indexing**.

---

## 📝 كيف تنشر مقالاً (الاستخدام اليومي)
1. ادخل لوحة المؤسس → تبويب **📰 المدونة**.
2. اكتب موضوعاً في «✨ توليد مقال بالذكاء الاصطناعي» (وعيّن المعيار/التصنيف اختيارياً) ثم **ولّد المسودة**.
3. راجع المحتوى في المحرر، عدّل ما تشاء (العنوان، الـslug، SEO، الوسوم)، وتابع المعاينة الحيّة.
4. اضغط **💾 حفظ كمسودة** للمراجعة لاحقاً، أو **🚀 نشر المقال** لينشر فوراً على `/blog`.
5. يظهر المقال مباشرة على المدونة العامة ويُضاف تلقائياً لـ`sitemap.xml`.

> يمكنك أيضاً كتابة مقال يدوياً بالكامل عبر «📝 اكتب مقالاً يدوياً».

---

## 🔍 روابط مهمة للتحقق بعد النشر
| الصفحة | الرابط |
|---|---|
| فهرس المدونة | `https://نطاقك/blog` |
| المقال الافتتاحي | `https://نطاقك/blog/loto-lockout-tagout-practical-guide` |
| خريطة الموقع | `https://نطاقك/sitemap.xml` |
| robots | `https://نطاقك/robots.txt` |

---

## ⚙️ ملاحظات تقنية
- مفاتيح Supabase العامة (anon) مضمّنة في دوال Edge — وهي **عامة بطبيعتها** (نفس المفتاح في التطبيق). الكتابة محمية بالكامل عبر RLS (بريد المؤسس في الـJWT).
- توليد الـAI يستخدم `gpt-4o` ومفتاح OpenAI المخزّن في `platform_secrets` (نفس مفتاح المنصة الحالي).
- إن غيّرت بريد المؤسس مستقبلاً، حدّثه في: سياسات RLS بقاعدة البيانات + ثابت `FOUNDER_EMAIL` في دالة `generate-article` + `murshid.html`.
